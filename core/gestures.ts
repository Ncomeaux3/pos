// Moved to holon-ui (holon-apple Phase 3). Kept so deep imports keep resolving.
export { type Swipe, MIN_DISTANCE, swipeOf, atTop, LONG_PRESS_MS, LONG_PRESS_SLOP, isLongPress, EDGE_PX, fromEdge, EDGE_BACK_PX, edgeBack } from '@ncomeaux3/holon-ui'
