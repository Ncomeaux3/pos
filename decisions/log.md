# Decision log (append only)

Format: date | decision | reason | alternatives rejected

2026-09-05 | Orchestrator is a fresh build inside the POS | keeps the POS self-contained and the agent surface small | reusing the owner's multi-agent framework
2026-09-05 | Notion POS stays live until each module replaces it | avoids a big-bang migration; Notion is the fallback | migrate everything in phase 1
2026-09-05 | Notion data comes in as an owner-provided export per module | one import script per module, tested against the export | live Notion MCP sync
2026-09-05 | Hosting: Supabase + Vercel | near zero ops, free tiers cover one user | self-hosted VPS behind Tailscale; local machine only
2026-09-05 | Stack: TypeScript, Next.js full stack | one language, one deploy, matches jobtracker and soltreya-web | Python backend with separate frontend; Next.js UI with Python jobs
2026-09-05 | Bank provider: SimpleFIN Bridge, 4 to 7 accounts | about $1.50/mo (verify), token API, no OAuth flow to build | Teller; Plaid; manual CSV
2026-09-05 | Cost cap: $0 to $10 per month | free tiers plus SimpleFIN plus small model spend | $10 to $30; $30 to $75
2026-09-05 | Mobile: PWA | one codebase, home screen install, email covers alerts | responsive only; native later
2026-09-05 | Fitness source: Strava plus Apple Health via Health Auto Export webhook | covers weight, sleep, heart rate; price verify | manual entry; Garmin/Whoop/Oura; defer
2026-09-05 | Obsidian sync: vault is a private git repo, app pulls and proposes commits | matches the rule that the app never edits the vault without review | Obsidian Sync plus local watcher; defer
2026-09-05 | Idea research depth: quick by default, deep on demand | cents per run, protects cost cap | deep by default
2026-09-05 | Notifications: email via Resend | free tier, no app, searchable | ntfy/Pushover; Telegram/Discord bot; in-app only
2026-09-05 | MCP layout: one server, namespaced tools | one process, one auth, add a module by adding a folder | one server per module; no MCP
2026-09-05 | Job runner: one Vercel cron hitting one API route | zero extra infra, daily cadence matches the digest pattern | pg_cron plus Edge Functions; in-repo scheduler; n8n
2026-09-05 | Template model: fork and fill | code holds nothing personal; data lives in DB, .env, imports/ | core plus pick-your-modules; build for owner then strip
2026-09-05 | Auth: Supabase Auth magic link, signups disabled, one owner user | no password, RLS backstop | email and password; passkey or Google OAuth
2026-09-05 | Models: Anthropic API, Haiku for classification, Sonnet for research, one wrapper function | cheapest capable, swappable | one model everywhere; provider-agnostic SDK from day one
2026-09-05 | Module plug-in: manifest file per module in modules/<name>/, discovered at build | the mechanism that makes the system adaptable | central registry file; database-driven registry
2026-09-05 | Integrations UX: Settings > Connections page with token forms and OAuth Connect buttons | plug and play accounts, no redeploy to add one | tokens only; keep credentials in .env
2026-09-05 | Integration plug-in: manifest file per provider in integrations/<name>/ | add a provider by adding a folder | hardcode the providers we use
2026-09-05 | Day one integrations: every planned provider defined, none connected | template ships empty and shows the full platform shape | only what Phase 1 needs
2026-09-05 | DB layer: supabase-js plus generated types, hand-written SQL migrations, no ORM | RLS applies automatically, least machinery | Drizzle; Kysely
2026-09-05 | Local DB: supabase start in Docker for dev and tests, one hosted project for prod | migrations and RLS proven before push | hosted only with a branch; one hosted project for everything
2026-09-05 | UI base: Tailwind plus shadcn/ui | owned components, restyled in the design pass without a library swap | Tailwind only; pick a kit during the design pass
2026-09-05 | Tooling: pnpm, Node 22 LTS | fast, strict, Vercel native | npm; Bun
2026-09-05 | Chat: Claude Code or Claude app via /api/mcp, no in-app chat | zero build, covered by the Max plan | in-app chat page; both
2026-09-05 | Template upgrades: forks add this repo as a git remote and merge | works because personal state is outside code | publish core as an npm package; no upgrade path
2026-09-05 | Notion import: every module except Skill Tree has Notion data to import | import scripts and fixtures are required per module | none
2026-09-05 | Git: init, first commit, private GitHub repo pos via gh | matches SETUP.md | init only; owner does it by hand
2026-09-05 | Schedule: one nightly run at 09:00 UTC (4 AM Central in summer, 3 AM in winter) | before wake, one cron entry fits the Hobby plan | separate sync and digest crons; evening summary
2026-09-05 | CI: GitHub Actions running typecheck, lint, vitest on every push | catches broken rule tests before deploy | Vercel build only; none
2026-09-05 | Deploys: Vercel Git integration, main to prod, branches to previews, branch per module | default behavior, no scripts | main only; manual CLI deploys
2026-09-05 | Skill tree edits: config/skills.yaml is the source, no edit UI | rare changes, version controlled | seed then edit in app; file plus form that commits
2026-09-05 | Backups: nightly pg_dump from GitHub Actions to a private backup repo, 30 days kept | free, restore is one psql command | Supabase Pro; manual dumps
2026-09-05 | Errors: Vercel logs plus core.jobs, no third party | job failures surface in the digest | Sentry
2026-09-05 | Demo seed: pnpm setup --demo seeds synthetic rows per module, doubles as fixtures | forks and the design pass see populated screens | empty start
2026-09-05 | Money: integer cents, USD only | no float drift; currency column added only when needed | numeric with currency code
2026-09-05 | Vector store: pgvector in Supabase with HNSW index | already in the database, RLS applies, free | dedicated vector DB; pgvector behind a swap interface
2026-09-05 | Embeddings: Voyage AI lite model (verify name and free allowance) | Anthropic's partner, cents per million tokens | OpenAI text-embedding-3-small; local model; Supabase gte-small
2026-09-05 | Search scope: everything in core.entities via one core.embeddings table and one search tool | one search across every module for owner and orchestrator | Second Brain notes only; per-module opt-in
2026-09-05 | Dashboard summary: templated by rules, one Haiku call writes a two sentence headline | numbers never hallucinated, cheap | fully model written; fully templated
2026-09-05 | Agent writes: direct for low risk, core.proposals for money, policy, goals; manifest marks guarded tools | safety where mistakes cost money | everything in review; everything direct
2026-09-05 | Levels: level = min(99, floor(sqrt(xp / 100))) | one line, easy to show on the skill page | thresholds table; linear
2026-09-05 | Embed timing: nightly job embeds new or changed entities | batched, fits cron and cost cap | on write sync; on write queued plus sweep
2026-09-05 | Digest email: short plain text, headline plus top 3 plus link | reads in two seconds on a phone | full HTML digest
2026-09-05 | Review queue: one core.proposals table and one Review page | one inbox for everything the agent wants to do | per-module pending status
2026-09-05 | Keyword search: Postgres full text merged with vector results | exact merchant names and tags still win | semantic only
2026-09-05 | Distribution: this repo, private now, public with Use this template later | personal state is outside the code | separate public template repo
2026-09-05 | License: MIT, file added now | standard for templates | Apache 2.0; decide later
2026-09-05 | Query tool: read-only SQL as restricted Postgres role pos_readonly, SELECT on own schema plus core, 5s timeout, row cap | flexibility for chat, safety enforced by the DB | structured filters; named queries
2026-09-05 | Files: Supabase Storage, one private bucket per module, signed URLs via core/files.ts | same auth and project, 1 GB free (verify) | links only; defer
2026-09-05 | History: imports backdate events to original dates; rules classify on import, model handles leftovers nightly | skill tree reflects real history, cost stays batched | no events; manual XP seed
2026-09-05 | Spend: core.llm_calls ledger per call, month to date on dashboard, soft cap in settings pauses research | keeps the $10 cap visible and enforced | log without cap; provider dashboards only
2026-09-05 | Settings: core.settings key-value table for timezone, owner name, digest hour, llm soft cap | template users need these without touching .env | env vars
2026-09-05 | Deletes are hard; core.events keeps a title snapshot | soft delete doubles every query; the event log is the audit trail | soft delete
2026-09-05 | All alerts bundle into the one daily email via core.notifications | one email a day, never a flood | separate sends per alert
2026-09-05 | Migrations additive after ship; every module table carries external_id and source | idempotent imports, forks can merge upstream migrations | none
2026-09-05 | Rate limiting: Vercel Firewall at the edge plus per-route in-memory limiter, 60 per minute per IP | no new service, edge blocks floods | Upstash Redis; Postgres counter
2026-09-05 | Caching: Next.js cache with revalidate tags, digest tables as read cache, provider responses stored in DB, no Redis | nothing extra to run | Upstash Redis
2026-09-05 | Logs: core.jobs plus core.request_log for every API and webhook call, no drain | outcomes survive Vercel's short retention | log drain; core.jobs only
2026-09-05 | Recovery: RPO 24 hours, RTO a few hours, single region, restore drilled once in Phase 1 | matches nightly dump and cost cap | RPO 1 hour; rebuild from Notion
2026-09-05 | Convention change: migrations live in supabase/migrations, not db/migrations | Supabase CLI runs them natively | keep db/migrations
2026-09-05 | Convention change: modules/<name>/schema.sql dropped, migrations are the single source of schema | no duplicated schema definitions | keep schema.sql as documentation
2026-09-05 | Verified: Vercel Hobby cron minimum interval is once per day with per-hour precision (fires within 59 min of the hour), 100 crons per project | 0 9 * * * is legal; the digest arrives some time in the 09:00 UTC hour | more frequent schedules fail at deploy
2026-09-05 | Verified: Fluid compute is default on for new projects and available on Hobby, default and max function duration both 300s | the nightly job gets 5 minutes, not the legacy 10s/60s; set maxDuration 300 on the cron route | assuming the pre-April-2025 60s cap
2026-09-05 | Verified: Vercel DDoS mitigation and Attack Mode are free on all plans; the Bot Protection managed ruleset is off by default and its plan gate is not documented | Attack Mode is the guaranteed lever; confirm bot protection in the dashboard at deploy | claiming bot protection is available on Hobby without checking
2026-09-05 | Embeddings: voyage-4-lite at 1024 dimensions, 32k context, $0.02 per million tokens, 200M free tokens | default dimension, best recall, about 40 MB at 10k entities against a 500 MB free database | 512 or 256 via Matryoshka truncation
2026-09-05 | Verified: Resend free tier is 100 emails per day, 3000 per month, 3 domains, 30 day retention; an unverified account sends from onboarding@resend.dev only to the account signup address | Phase 1 needs no domain because the digest goes to the owner's own inbox | verifying a domain before Phase 1
2026-09-05 | Verified: Supabase free tier is 500 MB database, 1 GB file storage, 5 GB egress, 2 active projects, paused after 1 week of inactivity | the nightly cron keeps the project active; storage decision confirmed | none
2026-09-05 | Verified: pgvector ships on every Supabase plan including free and supports HNSW | no plan upgrade needed for search | dedicated vector service
2026-09-05 | Backups connect over the Supavisor session mode pooler on port 5432 | GitHub Actions runners are IPv4 only and the direct connection is IPv6 only; pg_dump needs session semantics that transaction mode on 6543 does not provide | direct connection; transaction mode pooler
2026-09-05 | MCP: mcp-handler 2.1.1 on @modelcontextprotocol/server 2.0.0 with zod 4, mounted as a Next.js route handler, bearer auth via withMcpAuth | serves the 2026-07-28 spec with stateless Streamable HTTP fallback, no hand-wired transport | raw @modelcontextprotocol/sdk 1.30.0 with a custom transport bridge
2026-09-05 | Verified: SimpleFIN Bridge costs $1.50 per month or $15 per year; a base64 setup token is claimed once for an access URL of the form https://user:pass@bridge.simplefin.org/simplefin | confirms the manifest stores access_url as the single token field | storing the setup token
2026-09-05 | DB layer refinement: core and module schemas stay unexposed to PostgREST; core/*.ts reads and writes over the pg pool, supabase-js is for auth only | one DB layer for module data, no per-module dashboard config to keep in sync, and nothing in core is reachable over REST from a browser; every path already runs server side behind requireOwner() or as service_role, which bypasses RLS regardless | expose core to PostgREST and use supabase-js with generated types; expose for reads and use pg for writes
2026-09-07 | Design handoff review: finish Phase 1 steps 8 to 16 before the handoff's new core tables | six of the designed screens have no runtime behind them, and steps 9, 10 and 12 are where the write guard, run context and job runner get designed | land the new core tables first as schema for the screens to bind against
2026-09-07 | Notifications stay email plus a new in-app read state; push dropped from the handoff's rules screen | web push is a subsystem (service worker, VAPID, per-device subscriptions) against an explicit Not planned decision; the nightly bundled email still carries everything | all three channels with quiet hours and urgent override
2026-09-07 | No modules.enabled setting; the onboarding wizard describes modules but never disables one | nav stays a synchronous read of the generated index, and deleting the folder stays the one way to remove a module | add the toggle and make nav async; enabled as a visibility filter only
2026-09-07 | Settings > Skills is read only over config/skills.yaml; the tree is edited in the file and redeployed | the Vercel runtime filesystem is read only, and moving the tree into a table gives classify() a second source of truth | core.skills table seeded by setup; YAML seed plus a DB override table
2026-09-07 | The Omelette design bundle is the visual and functional spec for every screen; docs/plans/design-build.md is the build plan | 22 high fidelity prototypes with final colour, type, copy and states, more specific than SPEC and than the backend handoff doc | treating the prototypes as inspiration and designing screens per module
2026-09-07 | Supersedes the email-only decision earlier today: push notifications get built, last | push appears in five screens (Notifications rules and lock screen preview, Settings channels, Onboarding presets, Tasks reminder channel, mobile badge); the rules table and every control ship now, the sender stays email plus in-app until a dedicated push step | email plus in-app only, cutting push controls from three screens; building the service worker and VAPID subsystem now
2026-09-07 | Supersedes the no-toggle decision earlier today: modules_enabled exists and means visible | Onboarding step 2 is ten module toggles and Settings and Notifications grey out module-off rows; nav, Onboarding and connector categories read it, while jobs, tools and direct URLs still work, so deleting the folder is still the real removal | no toggle at all; a full disable that stops jobs, unregisters MCP tools and 404s the pages
2026-09-07 | Supersedes the read-only decision earlier today: config/skills.yaml seeds, core.skill_overrides holds edits | the Settings Skills tab renames, deletes, restores and adds custom skills, and Tasks creates a skill inline; the prototype's localStorage store has exactly the custom, renames, deleted shape, and the Vercel filesystem is read only at runtime | read only screen, edit the YAML and redeploy; moving the whole tree into core.skills and losing the fork-and-fill config file
2026-09-07 | Insurance gets discrete deductible, coverage limits, agent and documents fields; SPEC section 8 amended | they are facts copied off a declarations page, not judgements; SPEC's real rule survives, which is that nothing analyses them, no gap analysis and no model opinion on adequacy | one free-text coverage_summary as SPEC wrote it; deferring the PDF extraction flow separately
2026-09-07 | Cross-module reads go through core.digests plus a named metric registry on the module manifest | Health reads Insurance and Finance numbers, Home reads mortgage and equity, Goals computes from four modules; digests are already nightly, which is the freshness the designs show, and no core object ends up referencing a module schema | a core.upcoming view and cross-schema SELECTs for reads only; live in-process summary tool calls between modules
2026-09-07 | Build order stays ARCHITECTURE's: Skill Tree, then Tasks and Goals, then Finance | Skill Tree is now mostly UI because classify(), core.skill_xp and core.skill_links already exist, and it unblocks the skill picker Tasks needs | Finance first per the backend handoff doc; Tasks and Goals first for dashboard tiles
2026-09-07 | Charts are hand-rolled inline SVG; d3-geo, topojson and world-atlas arrive only with Travel | each of the nine chart types is 30 to 80 lines and matches the tokens exactly, and four of them (radar, constellation, heat strip, month strip) would be custom under any library anyway | Recharts or visx now; numbers first with charts in a later polish pass
2026-09-07 | SPEC features the designs omit stay in SPEC as a follow-on pass per module | Ideas research rubric, Meals cook mode, Travel cents per point, Fitness plans and coach are absent from the prototypes but are why those modules exist; Ideas without its rubric is just a kanban | dropping all four so the design is the whole spec; building them in the same pass with invented UI
2026-09-07 | Mobile is responsive first: every screen reflows to 402px, sidebar becomes a bottom tab bar, drawers become sheets; the four gestures land in one later pass | reflow is nearly free if the primitives are built responsively, and retrofitting it across 22 screens later is worse; which gestures matter is only knowable once the PWA is on the phone | building gestures with each module; desktop only until every module ships
2026-09-07 | Verification is a Playwright smoke test per screen with 1440px and 402px screenshots in both themes against the demo seed | the repo rule is evidence not claims, and nothing else catches a screen that breaks at 402px or after a token change | vitest and manual browser checks only; committed visual regression baselines that fail CI on any pixel change
2026-09-07 | Autonomy is one core.settings key (observe, propose, act) read by the guard dispatcher | the dispatcher already decides guarded or not in step 9, so this is three lines in code being written anyway, and observe only is genuinely useful for the first weeks of a new module | a read-only selector with the runtime override deferred; dropping the selector and keeping manifest guards only
2026-09-07 | Phase 1A builds the tokens, shell and all twenty primitives in one step before any new screen | every screen after it becomes assembly rather than invention, and no primitive gets reshaped across nine screens because the tenth needed it different | tokens and shell now with primitives created as each screen needs them; splitting the foundation across two steps
2026-09-07 | Second Brain's schema id is brain, not second_brain; SPEC amended | matches ARCHITECTURE's repo layout, the design's brain.write and brain.commit tool names, and the sidebar; shorter in every query, tool name and URL | second_brain per SPEC and the Goals metric source strings
2026-09-07 | Login keeps one reply for every address; the design's denied view is not built | telling a visitor that an address is not the owner answers exactly the question an attacker on a public URL is asking, and /not-owner already covers the case after a real login | build the denied view as designed; show it only on a private network
2026-09-07 | Email OTP expiry cut to 900 seconds to match the login screen's 15 minute countdown | a sign in link valid for an hour in a mailbox buys nothing when the owner is at the keyboard, and the number on screen should be true | keep an hour and reword the screen; drop the countdown
2026-09-07 | Owner access is enforced in proxy.ts, not only by requireOwner() in the (app) layout | redirect() in a layout does not stop the page beneath from rendering, and Next serialises that render into the 307 body, so a signed out GET of /settings/connections was handed provider names, the webhook URL and the webhook shared secret | requireOwner() in every page; accepting the leak because the browser discards a redirect body
2026-09-08 | The ComeauxVerse brand guide governs the app, not only the content surfaces: Manrope one family, no monospace, radius 12px standard and 8px small controls, and the brand type scale | one system from a thumbnail to the dashboard; the palettes were already identical, so only type and shape changed | keeping the POS bundle's Space Grotesk plus JetBrains Mono and radius 0 inside the app; writing the split into brand/visual.md as two systems
2026-09-08 | Teal #2FB8A6 added as a fifteenth token for accent text under about 14px | the green measures 3.80:1 on the dark ground, which is AA for large text only, and the nav codes, chips and badges were all below that; green keeps borders, fills, bars, rules and the mark spine | leaving the green on small text as the POS prototypes do; dropping accent colour from small text entirely
2026-09-08 | The ComeauxVerse mark and lockup replace the CMX Logic logo, inlined as SVG with the arms on currentColor and the spine on the accent | the mark then follows the theme instead of shipping a white-only and a dark-only file, and the brand rule that only the spine carries accent is expressed in code | referencing the exported SVG files as images; drawing a new mark
2026-09-08 | One deviation from "no monospace anywhere": literal secrets and shell commands render in the system monospace stack via .code | a base64 webhook secret or an access URL is checked character by character, and proportional digits make that harder; no webfont is downloaded, so the one-family rule still holds for anything that is type | rendering secrets in Manrope like everything else
2026-09-08 | Search indexes text and vectors in two halves: full text always, embeddings only when Voyage answers | tsv lives on core.embeddings, so a single combined pass meant search found nothing at all until a Voyage key was pasted; content_hash tracks the indexed text and a null embedding marks what still needs a vector, so rows indexed before Voyage arrived are picked up on the first run after it | one pass that fails or no-ops without Voyage; computing tsv in the query with no index
2026-09-08 | Integration clients import getCredentials lazily inside the call rather than at module scope | a manifest imports its own client and is itself reached through core/integrations, so the static import was a cycle; Next's bundler hoists around it and plain Node does not, which broke every entry point that is not the app | moving the shared constants to a third file; leaving the cycle since the app happened to work
2026-09-08 | The Playwright suite seeds and indexes its own fixture in a setup project | the vitest suites truncate core.entities, so the e2e run was passing on state a previous command happened to leave behind and failed the moment the two ran in the usual order | seeding by hand before an e2e run; making vitest restore what it truncates
2026-09-08 | Verified: Voyage limits an account without a payment method to 3 requests per minute and 10K tokens per minute | every semantic search costs one request, so the fourth search in a minute silently fell back to word matches; query embeddings are now cached in process, the command palette does not embed at all, and the Search page says when it degraded | leaving the silent fallback; embedding in the palette on every keystroke
2026-09-08 | No monospace anywhere, including secrets and shell commands; machine values get tabular figures, slashed zero and slight tracking in Manrope instead | the brand guide says one family and means it, and the readability argument for monospace is answered well enough by figure settings | keeping a system monospace stack for secrets only
2026-09-08 | getConnectionStatuses derives connected from status = 'connected' rather than from a row existing | a provider whose key the API rejected was rendering as Connected with "401 Unauthorized" printed underneath it, which is the one place a status must not be optimistic; the card now shows Rejected, and Test and Disconnect stay reachable so a bad key can be replaced | leaving the boolean as "a row exists" and reading the detail line
2026-09-08 | Tests run against a pos_test database on the same local Postgres, rebuilt from supabase/migrations by a vitest globalSetup, with core/db.ts refusing any other database while VITEST is set | three suites delete from core.connections and core.settings, so pnpm test was disconnecting every provider and resetting preferences against the development database; it destroyed a real Voyage key on 2026-09-08 | a guard with no second database, which would skip those suites; suites that snapshot and restore, which lose the data on a crash
2026-09-08 | The vector arm of search takes a cosine distance floor of 0.7 | ranking by distance always produces a ranking, so once Voyage was connected every query matched every row and the no-results state became unreachable; measured on the demo corpus, a true match sits at 0.504 and the nearest unrelated note at 0.750 | no threshold, relying on reciprocal rank fusion to sink the noise, which it cannot do when only one arm returns anything
2026-09-08 | Search runs full text first and buys an embedding only when words return nothing | Voyage allows 3 requests a minute on the free tier and every embedded query spends one; a hit on the words is already an answer, and meaning only earns its cost when words have failed. Measured over eight representative queries this went from 8 requests to 2, and the two spent are exactly the ones words could not answer | embedding every query, which burned the minute budget in four searches; a count-based threshold, which is wrong because how many hits count as enough depends on corpus size
2026-09-08 | An in-process courtesy limiter matching the free tier ceiling sits in front of the embedding call | a burst otherwise spends its requests on 429s, paying the latency and getting nothing back; the fourth query inside a minute now falls back to words immediately. Per process, so approximate on serverless, and the try/catch remains the real safety net | relying on the provider's 429 alone
2026-09-08 | Search reports a mode of text, hybrid or degraded rather than a semantic boolean | choosing not to embed because words answered the query is not a degradation, and the warning banner should only appear when meaning was wanted and could not be had | a boolean, which made a deliberate saving look like a failure
2026-09-08 | New migrations are applied locally with supabase migration up, never supabase db reset | reset rebuilds the database from migrations, which deleted the owner user and all three provider keys on 2026-09-08; the keys are encrypted and exist nowhere else, so they could not be restored and had to be pasted again | continuing to reach for db reset out of habit
2026-09-08 | Autonomy has three levels: observe proposes everything an agent does, propose applies the manifest's guarded list, act holds nothing back | act is safe because every write still lands in core.write_log with its before value and the Agent Log can undo it; without that it would just be off | making act equal propose, which would leave the level meaningless; a second manifest list splitting always-guarded from review-guarded, which is two lists to keep in step
2026-09-08 | core/tools.ts callTool is the only way an agent reaches a module tool, and it validates the payload before storing a proposal | a proposal the owner approves must be one the tool will accept, or approval fails later for a reason they cannot see; and a guard only guarantees anything if there is no second path around it | letting MCP and the orchestrator call manifest.tools directly and guarding at each call site
2026-09-08 | Dismiss sets dismissed_until 30 days out rather than rejecting outright, and pending hides anything still inside its window | the design says a dismissal means not this again for a while, and a status change alone would relabel the row without quieting the inbox | a hard reject; a boolean dismissed flag with no expiry
2026-09-08 | core/proposals.ts imports the module registry lazily inside approve() | the registry pulls in every manifest and a manifest pulls in its React pages, so a top level import made the module unloadable outside Next and broke the e2e seed, which only wants propose(). Step 12 needs the registry to load in a plain Node process and that is where it gets solved properly | importing at module scope and giving the seed raw SQL instead
2026-09-08 | The unclassified marker is deleted as soon as a real skill is found, unless the owner set it by hand | it is a parking space, not a skill: left in place it sits in the Skill Tree forever and earns XP of its own. Every row classified while Anthropic was disconnected carries one, and the nightly batch would otherwise leave the marker beside the real links | leaving it and filtering the marker out at read time in every consumer
2026-09-08 | The query tool refuses a data-modifying CTE by name, on top of the pos_readonly role | `with x as (delete ... returning *) select * from x` is a legal SELECT by shape and Postgres will run it; the role stops the write regardless, but refusing early gives an agent a message it can act on rather than a permission error it cannot | trusting the role alone; parsing the SQL properly, which is a dependency for a check a keyword scan handles
2026-09-08 | The row cap wraps the query in a subselect rather than appending a limit | appending `limit 500` to a query already ending in `limit 10000` does nothing, and telling whether an existing limit is the outer one means parsing SQL | appending, or regex detecting an existing limit
2026-09-08 | Storage buckets are created on demand by core/files.ts, not by a migration | buckets are not schema, and a module that never stores a file should not have one; private always, so a missed policy cannot expose a health record | declaring them in supabase/config.toml, which cannot know about a module added later
2026-09-08 | The request log stores a truncated hash of the caller address, never the address | a burst is still traceable across routes, and the log does not become a list of who called | storing the raw address; storing nothing
2026-09-08 | Vocabulary a client component needs lives in a leaf module with no imports: core/owner.ts for the access rule, core/autonomy.ts for the levels | core/tools.ts reaches pg, the module registry and through it every module's React pages, so a client component importing a type from it drags the whole server graph into the browser bundle. Turbopack reported that as a missing build manifest, not as an import error, which cost time to find | importing the type from core/tools and letting the bundler sort it out
2026-09-08 | The MCP token is not rotatable from the app, and the screen says so | MCP_TOKEN is an infrastructure secret in .env, and the app cannot write its own .env; a Rotate button would either lie or split the secret across two places | storing an override in core.settings that takes precedence over the env var
2026-09-08 | Both registries are split into a leaf contract and a registry: core/module-contract.ts and core/integration-contract.ts hold the types and the define functions, core/modules.ts and core/integrations.ts hold the lookups | a manifest imported defineModule from the registry, and the registry imports the manifests through _index, which is a cycle; Next's bundler hoisted around it and plain Node did not, so the registry could not be loaded by a script, a job outside Next, or CI. This is what blocked steps 12 and 13 | working around it per caller with lazy imports, which is what had been done twice already
2026-09-08 | Stored credentials live in core/credentials.ts, not in the integration registry | getCredentials needs only db and crypto, never the list of providers; reaching it through the registry made manifest to client to registry to _index to manifest a cycle, which is what forced the lazy import in the voyage and resend clients | keeping the lazy import workaround
2026-09-08 | The digest recipient is a core.settings key (digest_email), separate from OWNER_EMAIL | OWNER_EMAIL is the login and changing it locks the owner out; the recipient has to be able to differ because Resend without a verified domain delivers only to the address the Resend account was created with, which is not the login address here | reusing OWNER_EMAIL as the recipient, which is what the 2026-09-05 note assumed
2026-09-08 | A digest email that is refused fails the job; one that cannot be sent because nothing is configured skips quietly | a digest that silently stopped arriving is the failure mode this system cannot afford, because the owner would not notice. Not configured yet is different: the rows wait and go out on the next run | treating every send problem the same way
2026-09-08 | The orchestrator queues one notification per run on channel 'digest', and supersedes any earlier one that is still unsent and unread | four presses of Run now put four near-identical warnings on the dashboard, and a week of unread nights would bury whatever is actually new. Sent ones stay, because those are history, and module alerts on the default channel are never touched | one notification per alert; deduplicating by date, which would also delete a module's own alert

2026-09-08 | `<module>.query` is scoped to its own schema plus core by a text check in `core/query.ts`, not by a role per module | The review of phase-1-core found `moduleId` was dropped in `core/tools.ts`, so `notes.query` would have returned every Finance row once that module shipped, while the MCP tool description claimed otherwise. A readonly role per module would let Postgres enforce it, but `pos_readonly` reading every schema is what `core.search` depends on, and a role per module is boilerplate in every future migration. The check unquotes identifiers before stripping literals, since `"finance"."tx"` would otherwise be removed whole, and rejects only prefixes matching a real other-module schema, because telling a schema from a table alias needs a parser | Rejected: a `pos_readonly_<module>` role per module; passing `search_path`, which does not stop a qualified name

2026-09-08 | The query tool refuses Unicode escape syntax (`U&"..."`) outright rather than decoding it | An automated review of the previous commit found that `U&"\0066inance".transactions` is legal Postgres that resolves to the `finance` schema, and no text based check can see it: unquoting leaves the escape sequence, so `assertSchemas` matches nothing and the scoping is bypassed. Nothing an agent legitimately queries needs the syntax | Rejected: writing a Unicode escape decoder, and parsing with libpg_query, which is a native dependency for one check

2026-09-08 | Skill Tree becomes its own module with a `skills` schema. Only `core.entities`, `core.events` and `core.skill_links` stay in core | The design handoff §2 lists Skill Tree among the modules that get own schemas and its §6 diagram shows `skills` as a module schema, SPEC agrees, and CLAUDE.md says a module is one folder with one manifest. Phase 1 put the tree, the XP weights, the level function, the xp view and the overrides table in core, which was wrong and gets more expensive with every module written against it | Rejected: leaving it in core as a screen over core data
2026-09-08 | `register()` classifies through an optional `classifier` field on the module manifest, resolved by a dynamic import of the registry | Core must not import one specific module, and a static import of the registry from `core/entities.ts` closes a cycle back through `modules/_index.ts`, which is the failure that broke `pnpm setup` and the cron job on 2026-09-08: it works under Next and not under plain Node. Deleting `modules/skills/` now leaves register(), events and every other module working | Rejected: core keeping the tree, which half honours the handoff; and core emitting for the skills module to classify later, which empties the skill chips the Tasks and Goals screens show at creation time
2026-09-08 | The committed tree stays generic; Nick's real tree is rows in `skills.override` | The repo is a fork and fill template that holds nothing personal, and editing a skill should be a click rather than a deploy. The nightly pg_dump covers it | Rejected: committing the real tree to the yaml for the sake of a git history of it
2026-09-08 | Skill XP starts at zero. No Notion backfill in the Skill Tree step | It is about a third of the module's build time and it duplicates what the Tasks and Second Brain importers will do, which is where the real event volume is. The tree fills as the app gets used | Rejected: importing the Notion export now to open the tree with real levels

2026-09-08 | `register()` emits the default `<type>_created` event only when the upsert inserted, detected with `xmax = 0`. A caller that passes an explicit `eventType` always emits | Building the Skill Tree screen made the consequence visible: every `pnpm setup:demo` and every e2e seed re-registered the same rows and awarded their XP again, so five demo notes carried thirty-eight creation events each and every number on the screen was wrong. A task completing or a book finishing is genuinely a second event on the same row, which is why a named event still fires | Rejected: leaving it to the later `update()` step as the plan said, which would have shipped a Skill Tree whose XP counted re-seeds

2026-09-08 | The Settings Skills tab renders only while a module declares a skill tree, decided by `getModule('skills')` inside `settingsTabs` | The seam is only real if deleting `modules/skills/` leaves nothing pointing at it. A tab that survives and explains its own absence is a dangling reference in the UI. Putting the check in `settingsTabs` rather than in each page keeps three callers from drifting | Rejected: a `hasSkills()` helper copied into each settings page
2026-09-08 | `skills.write` merges into the override row with `coalesce` rather than replacing it | Rename and keyword edits both land on one row keyed `(kind, skill_id)`. A replacing upsert erased whichever edit came first, silently, so renaming a skill dropped its keywords and the skill stopped matching any rule | Rejected: separate rows per field, which makes the merge in tree.ts harder for no gain
2026-09-08 | Keywords are one editable comma separated line, not chips | The prototype shows chips, but chips plus an editor meant rendering the same keywords twice on every row. Keywords decide cost, since a skill with none never matches a rule and sends everything to the model, so being editable matters more than the chip treatment | Rejected: chips plus a separate edit control
2026-09-08 | Insurance reads a declarations page by sending the PDF to Anthropic as a document block, with no local PDF parser | the model already reads PDFs, so the alternative was a dependency and a text extraction step for something one content block does; the file is stored in the module's private bucket and the screen says so before the upload happens | pdf-parse or unpdf locally, keeping the page off the wire; typing every policy in by hand
2026-09-08 | Home stores maintenance as an interval plus the date it was last done, never a list of future dates | a row of dates goes stale the first time a job happens early or late, and reconciling it means rewriting the future or living with a calendar that disagrees with the history | a scheduled_jobs table with rows for the next year, regenerated nightly
2026-09-09 | The Fitness coach is deterministic rules, not a model call | a layoff, a load spike, a stalled lift and a short week are four comparisons over numbers the digest already has, and the same Sunday should not produce different advice twice; it also costs nothing against a $10 cap | a weekly Haiku call over the training log; a coach that only summarises and never suggests
2026-09-09 | write_plan is Fitness's only guarded tool, and the coach reaches a plan through nothing else | SPEC says proposals only and the owner approves, so the guard is what makes that true rather than a convention; a workout and a body metric stay unguarded because they are records of what already happened | an unguarded coach that edits the plan and logs the change to the Agent Log for undo
2026-09-09 | callTool forwards title, confidence, evidence, affects and diff to propose() | without it every guarded agent call produced a proposal titled `module.tool`, which tells the reader which function wanted to run and nothing about what it would do; the Review row is the whole decision surface | leaving proposals title-less and reading the payload; a per-module title convention
2026-09-09 | Cook mode scales only a leading number and says what it could not scale | quantities are free text because recipes say "a splash" and "2 cloves", and the first number is the count while later ones are sizes: "2 x 180 g" doubled is four fillets, not four double-sized ones | parsing quantities into number plus unit at write time; refusing to scale at all
2026-09-09 | Ideas research uses the Anthropic web search tool, and a claim whose URL the search did not return is deleted | verified 2026-09-09 that search is $10 per 1,000 plus the tokens the results cost, and a measured quick run came to about twenty cents; the deletion rule is enforced in code because a model told to cite everything still produces a confident number with a URL it invented | prompting for citations and trusting them; a separate search provider; no web search and therefore no numbers at all
2026-09-09 | research is guarded, alone among the Ideas tools | it is the only one that spends money, and an agent researching forty ideas one night would be inside the monthly cap and still wrong | unguarded with the soft cap as the only brake
2026-09-09 | Web push ships with VAPID keys in .env rather than core.connections | they are infrastructure like CRON_SECRET, not an account credential fetched through getCredentials(); the push service is not a provider the owner has an account with | storing them encrypted in core.connections beside the provider keys
2026-09-09 | Push fires only for rules that ask for it, once per send | a notification with no rule behind it was raised by a job rather than chosen, and pushing those makes the one channel nobody can ignore the one nobody chose; one per send for the same reason the digest is one email | pushing everything pending; one push per item
2026-09-09 | Gestures are touch and pen only, and the swipe decision lives in core/gestures.ts | a mouse drag across a task list is a text selection, and completing a task because someone highlighted its title is how a gesture gets removed rather than fixed; the pure half sits in core because vitest only collects from core, modules, integrations and scripts | a gesture library; treating any pointer drag as a swipe
2026-09-09 | Long press to arrange the dashboard is not built | the dashboard has no stored tile order, so the gesture would have nothing to arrange; building the ordering system to justify the gesture is backwards | building tile order and the gesture together now
2026-09-09 | No Notion importers are built. Notion stays the archive and POS starts empty | the owner chose it; without a real export every importer would be written against a guessed column layout, and SPEC's own convention asks for a test against a sample that does not exist | a generic CSV importer with hand mapping; guessing the export shape from the docs

2026-09-09 | Skill classification runs rules in the write path and the model in a nightly `reclassify` job, batched 20 entities per call | measured, not assumed: 3,919 of the 3,977 model calls this system has ever made were classification, and they came from `setup:demo` and e2e re-classifying the same 103 rows because `register()` calls the classifier on every upsert while only guarding the event with `xmax = 0`; the tree and instructions were also resent per entity, about 90% of each call's input. It also builds the job the `unclassified` marker's own comment already promised, which never existed, so 27 rows were parked with nothing to ever pick them up | Rejected: a content_hash column on core.entities, which the marker already does for free once the queue has a consumer; prompt caching, verified inapplicable because the prefix is ~190 tokens against a 512 floor; the Batch API's extra 50%, because up to 24h on top of the nightly run puts a skill link two days behind the write
2026-09-09 | The dashboard headline is a template, not a Haiku call | it is the most read sentence in the app and it was the one place a model could put a number in front of the owner unprompted; a template costs nothing, works with no provider connected, never counts against the cap, and is testable rather than sampled | Rejected: keeping the model behind a setting, which is two code paths for one sentence
2026-09-09 | skills.yaml gains `home` and `preventive_care` leaves and wider keyword lists | Home is the largest module in the app and had no skill to land on at all, so all 26 of its entities went to the model; measured against the real entity set the rule hit rate went from 31% to 58%, and Home's misses from 28 to 7. Keywords are the cheapest thing in the system and were carrying the least weight | Rejected: leaving the tree alone and letting the nightly job absorb the volume, which pays a model to answer what a word match already knows
2026-09-09 | `goals.checkin` refuses an automated write onto a manual row | the nightly `pullMetrics` writes `is_manual: false` onto today's row, which is the same row the owner may already have typed by hand, and `set value = excluded.value` took the number back while the flag still said manual; proven with a failing test before the fix | Rejected: having pullMetrics skip manual goals in its own query, which leaves the same trap for the next caller

2026-09-09 | The integration contract gains an optional `prepare()`, run on save and never on the Test button | a SimpleFIN setup token can be claimed exactly once, so claiming inside `test()` would store the spent token and the next Test press would 403 with the working access URL already gone; core cannot special case a provider, so the seam belongs on the manifest | Rejected: claiming inside test(), which destroys a one-shot credential on the second press; making the owner run a curl to claim the token by hand
2026-09-09 | `finance.sync_simplefin` skips any account whose currency is not USD, and says which | `finance.account` has no currency column and net worth is a plain sum over `balance_cents`, so a euro account would be added to a dollar one as though they were the same unit; a wrong net worth that looks right is worse than a missing account that announces itself | Rejected: storing them anyway and hoping; adding a currency column and FX rates, which is a module's worth of work for an account nobody has yet
2026-09-09 | `register()` gains `emit`, and the vault pull passes false | pulling four hundred notes written over five years is not four hundred notes of work tonight, and emitting per note would award all of them today's XP; the row still has to register or search cannot reach the vault at all, which is the whole point of pulling it | Rejected: not registering vault notes, which makes search blind to the vault; backdating to the file's last commit, which is one API request per file against a 5,000 an hour limit
2026-09-09 | The vault pull compares git blob shas and reads nothing else | git has already hashed every file, so an unchanged sha is an unchanged file and a nightly pull over a settled vault costs one request; the content hash the classification work went looking for already existed here | Rejected: a content_hash column of our own; reading every file every night and comparing text
2026-09-09 | A note whose vault file has gone is counted, not deleted | a rename, a move or an accidental delete would otherwise silently remove the owner's note, and the app is not the last copy of a git repo | Rejected: deleting to keep the mirror exact; a tombstone column, which is a status for something nobody has asked to query

2026-09-09 | Readability extraction is hand rolled in modules/brain/extract.ts, with no parser dependency | the module shows the extracted text beside the draft, so a bad extraction is visible and correctable rather than silent, which is a far lower bar than a library has to clear; it also keeps jsdom or linkedom out of a serverless bundle | Rejected: @mozilla/readability plus linkedom, two dependencies and about a megabyte for a page the owner reviews anyway; sending raw HTML to the model, which is 5 to 20 times the tokens and can hallucinate through a paywall stub
2026-09-09 | YouTube transcripts read the caption track list off the watch page. SPEC's yt-dlp is not used | yt-dlp is a Python binary and Vercel's Node runtime cannot run one, so the choice was between this and no transcripts at all; this is what yt-dlp does for captions anyway. The endpoint is undocumented, so every parser returns nothing rather than guessing and a video without captions says so | Rejected: yt-dlp, which cannot run on the target platform; pasting a transcript by hand every time
2026-09-09 | `brain.ingest` is guarded, alone in a module whose README said nothing needs guarding | the original reasoning holds for every other tool, the draft state is already the review step, but it does not cover money: ingest spends a Haiku call per URL and an agent reading forty links one night would be inside the cap and still wrong. Same rule Ideas applies to research | Rejected: leaving it unguarded because the draft state exists, which guards the wrong thing
2026-09-09 | `summary` is a capped purpose alongside `research` | both are owner initiated and both degrade rather than break: a draft still arrives with the full source text and a line saying why there is no summary, and writing the note yourself is what a second brain is for. Classification stays uncapped because it keeps the system working | Rejected: uncapped like classification, which risks a surprise on a bulk paste
2026-09-09 | Ingestion refuses a page with under 400 characters of prose rather than summarising it | a paywall stub, a cookie wall and a client rendered page are indistinguishable from here, and a confident summary of an article nobody read is the worst thing this feature could produce | Rejected: summarising whatever came back and trusting the owner to notice
2026-09-09 | `normaliseUrl` refuses private and link local addresses before fetching | it runs in a server action, so the URL is attacker controllable in the general case and the request goes out from inside the deployment; 169.254.169.254 is the classic target. A hostname that resolves to a private address is not caught, which is stated rather than implied | Rejected: no check, on the grounds that there is one signed in user; resolving and pinning the address through the fetch, which is a lot of machinery for a single user app

2026-09-09 | Ingestion follows redirects by hand, re-checking every hop, and resolves the hostname before fetching | an automated review caught the first version: `redirect: 'follow'` checked the pasted URL and nothing after it, so a page answering `302 Location: http://169.254.169.254/latest/meta-data/` was fetched from inside the deployment with the guard already satisfied, needing no DNS control at all. The lookup closes the neighbouring hole, a public hostname like localtest.me that simply points at 127.0.0.1. DNS rebinding stays open and is documented rather than papered over, because closing it means pinning the address through the connection and node's fetch cannot express that | Rejected: trusting the first check because there is one signed in user, which is the reasoning that produced the hole

2026-09-09 | Outbound HTTP in Second Brain uses `node:https` with a pinned `lookup`, not `fetch` | closing DNS rebinding means the address that was checked has to be the address that is dialled, and node's fetch has no way to express that: it cannot take a dispatcher without the undici package, and pinning by connecting to the IP directly breaks TLS certificate validation. `node:https` accepts a `lookup`, which `pinnedLookup` uses to answer with the already-checked addresses and ignore the hostname. Verified empirically before building on it, and there is a test with a real server so the mechanism is proven rather than assumed | Rejected: adding undici for its Agent, a megabyte of dependency for one option; connecting to the resolved IP with a Host header, which fails certificate validation; leaving rebinding open and documented, which was the previous state and is a real hole
2026-09-09 | The redirect loop takes its fetcher as a parameter | the address checks must not have a test-only bypass, and testing the loop against a local server would need one because 127.0.0.1 is exactly what the checks refuse. Injecting the fetcher tests the control flow with no network and no weakened guard | Rejected: an allowPrivate flag for tests, which is a security switch one careless call from being wrong in production

2026-09-09 | Owner facing clock reads go through `minutesIn`, `clockIn` and `dayIn` in core/today.ts, never `new Date().getHours()` | found live: the Agent Log showed tomorrow's date all evening and quiet hours were checked against the wrong six hours, because on Vercel the server is UTC and the owner is in Chicago. `inQuietHours` now takes minutes since midnight rather than a Date, so it reads no clock at all and its test does not depend on the machine it runs on. Intl does the conversion, so daylight saving needs no offset table | Rejected: a date library for one conversion; storing an offset with the timezone, which goes stale twice a year
2026-09-09 | The daily digest is exempt from quiet hours. Everything else still waits | there is one cron run a day and it fires at 03:00 or 04:00 local, inside any sensible overnight window, so a digest held for "later" would find that later is tomorrow's run, also inside the window, forever. The digest is also the thing the owner asked to receive rather than an interruption, which is what quiet hours exist to stop | Rejected: holding it like any other notification, which means never sending it; moving the cron out of quiet hours, which puts the dashboard write in the middle of the day

2026-09-10 | The 402px pass takes PosPhone.dc.html's shell measurements and leaves the app's own content alone | the artboard is one file covering four screens, and what it actually specifies that the app was getting wrong is chrome: 18px body padding not 28, a 56px tab row on a 26px home indicator inset, an 18px glyph where the rail uses a two digit code, a 44px search icon where the desktop has a field, KPIs two up at 24px rather than one per row at 34, and a segmented control that scrolls off the edge rather than wrapping onto two lines. Every one of those is measurable and none of them changes what a screen says | Rejected: reflowing each screen's content to its phone artboard, which only exists for four of the twenty-four and would leave the other twenty guessing
2026-09-10 | The quick add button the phone artboard floats over every screen is not built | it opens a sheet with a parsed task in it, and the app's add path is the inline field on /tasks; a floating button that adds a task from the Meals screen is worse than no button, and building the sheet to justify the button is the same mistake as the long press gesture with no tile order behind it | Rejected: a button linking to /tasks with the field focused, which is plumbing for an affordance nobody asked for; building the quick add sheet now
2026-09-10 | The phone dashboard keeps every module tile rather than the artboard's six | the artboard draws net worth, warnings, review, tasks, timeline and goals because that is the data its author invented; the app's dashboard is arrangeable and shows every module that writes a digest, so cutting it to six on a phone would hide the owner's own modules on the device they are most likely to read them on. A phone scrolls | Rejected: a mobile only tile allowlist, which is a second dashboard to keep in step with the first


2026-09-11 | The fidelity pass is done in the app, one screen at a time, Weekly Review first | the prototypes already live in Claude Design, so a second canvas copy would add nothing; the app is what the owner uses. The live project's `POS Weekly Review.dc.html` matches the Sep 9 export line for line, so the local handoff folder stays the source. Fidelity is pixel geometry in the app's own components, proven by prototype and app captures side by side plus structural e2e asserts, with no prototype markup and no visual-diff dependency | Rejected: a /design canvas duplicate; porting the .dc.html markup into React, which throws away components/pos; running the existing fidelity plan alone, which fixes three defects and leaves the rest
2026-09-11 | The review's glance shows the six tiles the artboard names, with deltas from last week's digest, and a tile whose digest is absent is hidden | the previous version showed whatever digest keys happened to exist, which is honest but is not the screen; a fixed order with real numbers is both. Finance, Tasks and Skills gain the three keys the tiles need, and the first week after deploy simply has no delta line | Rejected: keeping digest-driven tiles, which never match the prototype; a model-written glance sentence, which costs money to say something a template can say from the same numbers
2026-09-11 | Roll counts for the review come from `core.write_log` (kind `rescheduled`), which Tasks already writes | the artboard's "rolled 3 times" needs a number Tasks does not keep, and the nightly roll already logs every move, so the count exists; a counter column is a migration for a display line | Rejected: a roll counter on tasks.task; dropping the roll clauses, which leaves two lines that never match
2026-09-11 | The wins helper does not say wins "feed the Skill Tree", because they do not | closing a review emits one `week_reviewed` event and no XP; copy that claims otherwise is a lie the owner would read every Sunday | Rejected: awarding XP for ticked wins, a new behaviour with a weight decision behind it that nobody asked for
2026-09-11 | `/notes` leaves the numbered sidebar list; the route and the command palette keep it | the artboard's list is thirteen modules plus Review, and the notes stub was shifting every number after it on every screen | Rejected: keeping the row, which puts the whole rail off by one against the spec
2026-09-11 | Weekly Review keeps its header theme button and the sidebar keeps its Dark and Collapse rows, both as drawn | the owner asked for the chrome exactly as the artboard has it; both controls call the same `setTheme` action so there is one source of truth | Rejected: dropping the header button as a prototype-only affordance

2026-09-11 | The desktop dashboard draws the artboard's nine tiles first, in its order, then every other module's tile in rail order | the artboard's top three rows are the screen the owner sees on load and they now match; the 2026-09-10 reasoning still holds below the fold, where hiding a module's tile on the device most likely to read it would be a loss for a match nobody scrolls to see. Arrange covers all of them | Rejected: the artboard's nine alone, which hides nine modules; keeping digest order, which never matches the top of the page
2026-09-11 | The dashboard headline reads the artboard's three clauses from digests, with the alerts sentence as the fallback | it stays the 2026-09-09 template, no model call: net worth change from Finance, budgets past 80% with days left in the month from Finance, the longest-idle skill from Skills, each only when its digest carries the number. The net worth clause says "in 30 days" because that is the period the digest measures, and "mostly the brokerage" is not written because no digest attributes the move to an account | Rejected: a model-written headline; copying the artboard's month wording onto a 30-day number
2026-09-11 | The System tile's footer names the next run and nothing about backups | the backup runs in GitHub Actions and the app has no record of when or how many are kept; a line that reads as a fact from the app should come from something the app knows | Rejected: a settings field for the backup schedule, manual and stale the day the workflow changes

2026-09-11 | The Finance desktop loses its tab row; the drawers are the drill in, and the phone keeps PosPhone's four segments | the artboard is one overview with three drawers and no tabs, and the tabs were the build's, not the design's. Filing a transaction already lives on the drawers' rows, so nothing is lost there; a subscription due in the fortnight gets a small Cancel on its Upcoming row because the artboard gives cancel no home and MCP is not a UI | Rejected: keeping the tab row as a deliberate deviation on every Finance shot; dropping filing and cancel with the tabs
2026-09-11 | The budget alert threshold is a Finance setting, a single row in finance.settings, default 80 | the artboard's Budget limits drawer ends with the slider, and the constant was already read in four places (the flags, the KPI, the digest, the headline clause), which is what a setting is for. It lives in the finance schema because it is Finance's, not core's | Rejected: a key in core.settings, which has core naming a module's preference
2026-09-11 | The shared drawer takes the artboard's 520px, band with the crumb and ESC · CLOSE, title block and dimmed backdrop, for every screen | Finance and Insurance draw the same drawer, so it is the app's drawer; matching it per screen would drift | Rejected: a Finance-only drawer style
2026-09-11 | The limits drawer holds edits until Done; Reset discards, and closing with edits asks | the artboard's three controls only mean something if a change can be taken back before it lands; the budget drawer's own limit box keeps saving on change, which is what it did | Rejected: save as you go with a Reset that resets nothing

2026-09-11 | Skill Tree's attribute labels are cut to six letters in the pips and around the radar, as the artboard cuts them | the artboard draws ENGINE, BUSINE, COMMUN, HEALTH and LIFE O in both places and the app already did it on the radar; two treatments of the same five names on one screen was the worse of the two | Rejected: full names in the pips, which read better and match nothing
2026-09-11 | The Skill Tree opens on the top-gaining skill when the URL names none | the artboard's right pane is never empty, and the top gainer is the one skill the rest of the screen already points at; a click still writes the selection to the URL | Rejected: the empty state with its explanation, which was a paragraph where the design has a skill

2026-09-11 | The travel globe's land is a committed list of points sampled once from the world atlas; the hand-rolled projection draws it | the artboard draws the continents as a dot matrix, and a dot matrix is a list of points; sampling it once keeps the 2026-09-07 rule that d3-geo, topojson and world-atlas stay out of the bundle, and nothing loads from a CDN at runtime. Natural Earth is public domain | Rejected: the wireframe, which was the one visible gap left on the screen; adopting d3-geo and a 100KB atlas at runtime for one screen
2026-09-11 | Travel loses its page tabs: loyalty is the strip under the band with a Manage drawer holding the balances and the cents-per-point calculator, parsed bookings are approved in the trip drawer's Inbox tab, and the map is the globe on the page | that is where the artboard puts each of them, and the calculator keeps a home rather than being retired | Rejected: keeping the tabs under the artboard's page; retiring the calculator to MCP
2026-09-11 | Trips get budget lines, planned per category, with actuals summed from confirmed itinerary items by kind | the drawer's Budget tab is per category and the app had one figure per trip; the artboard's line about actuals filling from Finance transactions is not written, because nothing tags a transaction with a trip | Rejected: one line per trip, which is the tab with nothing in it
2026-09-11 | Travel's alert band shows an unread travel notification when there is one and nothing otherwise; the packing suggester is not built | the artboard's "flight dropped $140" needs a fare watch the app does not have, and "Suggest items" needs a model call with its own cost and guard; neither is a screen decision | Rejected: building either inside a fidelity pass
2026-09-11 | Not drawn on Travel for want of a source: the HOME pin, the check-in reminders footer line, and loyalty deltas before a second balance exists | each would be a fact the app does not hold; previous_balance on a loyalty program makes the delta true from the second edit on | Rejected: sample values in their place

2026-09-11 | A task row expands in place; the EDIT mini, the Edit button and a double click open the drawer, which is now a form that holds edits until Save | that is the artboard's two-level model, and the field-by-field save on blur was firing a write per field for what the owner reads as one edit; 480px because that is the width the artboard draws, beside the shared 520 and Travel's 560 | Rejected: the drawer alone with no expanded band; keeping save on blur behind a Save button that saved nothing
2026-09-11 | Skill links show on a task read only, with confidence and how they were classified, and names come through a `skillNames` seam on the module contract | core.skill_links holds ids and only the Skills module knows the names, so the seam sits beside `classifier`, which is the same kind of hole; the artboard's add-a-skill select, new-skill box and learned word rules are a classifier the app does not have | Rejected: Tasks reading skills.yaml, which has one module reading another's files; showing ids
2026-09-11 | Tasks gets a guarded `delete` tool; Delete on a row and in the drawer asks first | the artboard has Delete in both places and the module had no way to remove a row; guarded like Travel's delete_trip so an agent proposes rather than deletes | Rejected: an unguarded delete; leaving the row with no delete
2026-09-11 | "Remind me" is a select over remind_minutes with At the time, 30 min before and Day before; the Reminder channel cell reads the task_reminder rule in core.notification_rules | "Morning of · in digest" has no minutes-before meaning and the channel names are the rule's own, not a string the screen made up. If the rule is muted or gone the cell says Off | Rejected: the artboard's five options with one that stores nothing; a hard-coded "Push + in-app"
2026-09-11 | Not drawn on Tasks for want of a source: the +12 XP toast, the goal suggested from the project, the model-fallback explanations | XP is a weight times events resolved by the Skill Tree, not a number the click knows; nothing maps a project to a goal | Rejected: sample values in their place
2026-09-11 | The shared TabBar takes the DS tab every artboard draws: 1px underline on the rule, 13px, 8px 10px 12px padding, no gap, mono 10px count | seven artboards define the same tab with only the side padding varying, so it is the app's tab; a per-screen padding class carries the variation | Rejected: restyling only the Tasks tabs and leaving the other six screens on the old 2px underline

2026-09-11 | Linked tasks reach the Goals drawer through a `linked(entityRef)` seam on the module contract, which Tasks fills from goal_ref | the artboard lists a goal's tasks and Goals may not read the tasks schema; `metrics` and `skillNames` are the same kind of hole, so this is the third of a pattern rather than a new one, and a fork without Tasks gets an empty list | Rejected: Goals querying tasks.task; a link to the By goal view in place of the list
2026-09-11 | Goals gets a guarded `delete` tool; the drawer's Delete asks first | the artboard has it and the module had no way to remove a row; guarded like the others so an agent proposes | Rejected: archive as the only exit
2026-09-11 | The goal drawer's Agent proposals are pending core.proposals for module goals whose payload.id is the goal | the proposals table has no entity column, and the payload of a goals.write proposal carries the id it would patch, which is the truthful match; the section is absent when there are none | Rejected: a proposals column for one drawer section
2026-09-11 | The goal drawer edits as a form that holds until Save; metric source is the registry list with no custom query | the app resolves a metric by registry key, so a typed query string would be a field that resolves to nothing | Rejected: the artboard's "Custom query…" option
2026-09-11 | Fidelity passes run three screens at a time in worktrees, each agent measuring and listing its decisions first, Nick answering all three at once, then building; agents edit only their module, their plan and their own test block, shared pieces wait for the integrator | the module folders are file-disjoint and the test blocks do not overlap, so the only serial parts are Nick's eye and the shared files; a batch of three costs one review round and no merge conflicts | Rejected: one screen per session; agents free to edit components/pos, which would have three of them restyling the same tab
2026-09-11 | Adding a book or an article to Second Brain means it was read: `publish` emits `book_finished` or `article_read`, the Reading list shows FINISHED · date on every accepted row, and there is no finished column, finish tool or Mark finished button | Nick keeps no reading-in-progress list; a note enters when the reading is done, so a second state would be a migration for a distinction he does not make | Rejected: `brain.note.finished_at` with a `finish` tool
2026-09-11 | Second Brain keeps the band's one search box; the artboard's in-list semantic results are not built | the band search already runs the hybrid query, and a second box on the page breaks the one-query rule BandSearch states | Rejected: `?q=` results with score bars in the list
2026-09-11 | Second Brain copy never claims a commit to the vault: "Accept →", "Save", a Vault cell that says "Not in the vault. Nothing here writes to it." | nothing writes to the vault and there is no proposal path for it yet | Rejected: "Commit to vault →" and a Commits history that would be fiction
2026-09-11 | Second Brain's ingest drawer offers URL, YouTube, Book and Note; PDF, depth and prices are not drawn; the running state is one line | there is no PDF extractor in the Node runtime, one Haiku call has no deep mode, and an ingest with no progress events cannot tick four steps honestly | Rejected: a PDF kind that would need a dependency; four staged steps as theatre
2026-09-11 | Insurance says `post_to_finance` is a recorded intent ("{n} marked for Finance", "Finance does not draw it yet") | nothing in Finance reads the flag, so "posts to Finance" would be untrue; the cross-schema write that would make it true is a later pass | Rejected: the artboard's "premiums can post into Finance as recurring charges"
2026-09-11 | Insurance draws a payment schedule stepped back from the expiry by cadence and never writes "Paid" | the app stores no payments, so the dates are inferable and the paid state is not | Rejected: leaving the card out; a "Paid" row
2026-09-11 | Insurance's upload copy says the PDF goes to Anthropic with the owner's key, one Haiku call, nothing saved until confirmed | the artboard's "Text is extracted locally" is not what the app does | Rejected: the artboard's three local steps
2026-09-11 | Insurance drops both of the app's own adequacy disclaimers; `attach_document` locks the file path to the policy's own prefix; `delete_policy` is guarded and removes only files under that prefix | the artboard draws no judgment and needs no disclaimer to say so, the rule holds by construction; an unguarded tool that took any bucket path let an agent expose or delete another policy's file | Rejected: keeping the lede and drawer paragraphs; an unconstrained file_path
2026-09-11 | Meals has Week and Recipes tabs; the grocery list is a drawer grouped by recipe, and drafts are cards at the top of Recipes | the artboard's shape; the by-item merge and its tests are retired with it | Rejected: the old Grocery and Inbox tabs
2026-09-11 | Meals builds `import_recipe` from schema.org JSON-LD (no model, no fallback) and `fill_week` as a deterministic rotation (favourites first); the checked fetch moves from `modules/brain/fetching.ts` to `core/fetching.ts` so Meals can use it | the artboard has a paste-a-URL form and a Suggest week button and the app had no way to add a recipe at all; `recipe-scrapers` is Python and cannot run on Vercel; no module may import another, so the SSRF check had to live in core | Rejected: leaving both out; a model-written week; Meals fetching on its own
2026-09-11 | Meals draws only the kcal target (15 kcal a pound, from Fitness) and no protein, carbs or fat targets, XP amounts, Nutrition or Groceries budget links, pantry line, or drag and drop | none of those has a source in the app; a protein target is a Fitness decision | Rejected: bars against invented targets
2026-09-11 | The "Ate something else?" log is label-only into today's first empty slot; kcal and protein are not parsed | `plan_entry` has no macro columns and adding them is a migration for a field nobody has typed yet | Rejected: parsing "burrito 780 kcal 35p" into columns that do not exist

2026-09-12 | Ideas keeps its Research card inside the artboard's drawer | the artboard predates the rubric and draws no research; a built feature with sources, a verdict and a bill is not dropped for a drawing, the same reasoning that kept Cancel on a Finance row | Rejected: research through MCP only
2026-09-12 | "Draft task" on an idea writes a review-state task through core's callTool to tasks.write with source agent, the first module to call another module's tool | the artboard says drafts land in Review, and an agent-source task write is exactly that; it goes through the tool registry with the guard applied, never SQL, so the rule that modules meet only through core holds. The draft's id and title are stored on the idea so the card can say PENDING without reading the tasks schema | Rejected: leaving the Agent card out; Ideas inserting into tasks.task
2026-09-12 | Similar ideas and related notes come from core.embeddings, and are absent when nothing is embedded | the registry already embeds every entity's text when a Voyage key is set, so a cosine query in core is the truthful source; the merge band's percentage is that similarity. Merge keeps one idea, appends the other's pitch and tags, and kills the other with the reason "Merged into ..." | Rejected: a hard-coded example pair; a similarity job of Ideas' own
2026-09-12 | ideas.idea gains tags, stage_since and the draft columns | the artboard's tags and "in stage" age have no column, and updated_at moves on every edit, which is not the same as the stage changing | Rejected: tags parsed out of notes
2026-09-12 | The quadrant rule stays the app's two-axis split; "Filler" is relabelled "Fill-in" | the artboard scores medium/medium as BIG BET by a special case, which is a drawing choice the digest and Review already disagree with; the label is copy and the artboard's wins | Rejected: adopting the special case
2026-09-12 | Ideas gets a guarded delete; a killed idea is still kept | killing is the answer for an idea that was tried; delete is for a capture that was never one | Rejected: kill as the only exit

2026-09-12 | Health keeps the shared band above the artboard's title block and draws no LIGHT/DARK button | the artboard predates the band and every other screen opens with it; the sidebar's Dark row is the app's theme control, as the 2026-09-11 Weekly Review decision settled for header buttons | Rejected: a screen without the band; a second theme button
2026-09-12 | Log a visit is real: a future date writes an appointment, a past date writes a record through a new write_record tool, and the attachment is a real file into the module's private bucket | the artboard's form has a truthful path for each field once a records tool exists; cost on a past entry is kept in the record's fields because nothing posts to Finance | Rejected: a form that shows an attachment choice and stores nothing; "posts to Finance" copy
2026-09-12 | Health's rail and drawer actions are the app's: Mark done and Snooze 3 mo on a screening card, SCHEDULED with View appointment when an appointment of the same name is booked, Mark done in the appointment drawer | Request booking, Add to calendar and File to Second Brain have no path in the app, and a button that claims one is the kind of copy the rules forbid | Rejected: the artboard's labels over actions that do nothing
2026-09-12 | Health's vitals tiles are body weight from Fitness and the stored vitals with their provenance, with a delta against the previous reading; no wearable tiles and no "slightly high" | no module registers resting HR, sleep or HRV, and the module's rule is that nothing here offers an opinion about a number | Rejected: sample WHOOP and WITHINGS tiles; an amber judgment on LDL
2026-09-12 | Health's medications note is the count marked today and the lowest refill; Insurance & cost keeps the digest's lines; the drawer uses the shared 480px width | no job adds a refill task, the plan figures have no source, and a fourth drawer width for 40px is not worth a prop | Rejected: the agent sentence; Plan, Deductible, Out of pocket and HSA rows; a 440px Overlay

2026-09-12 | Settings is one page titled "Settings" on every tab, the tab as the crumb and "{n} of {m} connected" in the band throughout | that is the artboard; the app's per-tab titles were the build's | Rejected: keeping a title per tab
2026-09-12 | A connection card's second cell is generic (Connected since, or Token expires) | the artboard's per-provider figures would need each integration to expose a metric of its own and none does; a fork with other providers gets the same card | Rejected: core naming Anthropic, Voyage or Resend to compute a figure for each
2026-09-12 | Settings General: the cap is a slider, the schedule cell shows 09:00 UTC in the owner's timezone, and the third nightly cell is jobs registered, not backups | the artboard's backup line has no source in the app (the 2026-09-11 System tile decision) | Rejected: a backup count the app does not hold
2026-09-12 | Agents & MCP shows the token masked inline in the command with Reveal and no Rotate token; Autonomy stays as a card | the app cannot write its own .env and the page already says so; autonomy is a built control the artboard predates | Rejected: a Rotate button that could not rotate
2026-09-12 | The Notifications channel switches write the channel onto or off every rule; quiet hours has no off switch | the coarse switch is the rule writer applied everywhere, which is a real thing to do; the app has no state in which quiet hours are off, so a switch would toggle nothing | Rejected: a decorative channel switch; a quiet-hours switch bound to nothing
2026-09-12 | Settings Skills keeps keyword editing behind the artboard's keyword mark and keeps the two-press reset | keywords decide what classifies by rule and are worth editing; reset drops every override | Rejected: read-only keywords; a one-press reset
2026-09-12 | Home's Property card draws the property's own facts and the Insurance premium, not mortgage balance, rate, escrow or equity | Finance has no loan or mortgage account kind, so those rows have no source; adding one is a Finance migration, not a Home pass | Rejected: typed mortgage fields on the property
2026-09-12 | Home's KPI notes say what the numbers are: "House at 92% · your own estimate", "Your own figures" | the total is the owner's estimate by the module's rule and the app cannot know whether tax and insurance were typed into annual cost | Rejected: the artboard's "Tax and insurance included"
2026-09-12 | Home: Mark done has no Undo, a snoozed job stays in its month with a quiet dot, the snooze toast keeps "It comes back, it does not go away." | Mark done writes history and reschedules, which the artboard's session-state undo cannot reverse; the digest counts a snoozed job's cost | Rejected: an Undo that would need a reversal write; dropping snoozed jobs from the calendar
2026-09-12 | Fitness draws the setup card only when there are no workouts at all, with the Strava row alone; Apple Health is not drawn | the Health Auto Export webhook validates and writes nothing yet, so a row would claim a source; wiring it is an integration task | Rejected: a row marked "NOT WIRED YET"
2026-09-12 | Fitness has no XP column; the table is DATE, WORKOUT, TIME, SKILL | the weight is in `skills.xp_weight`, which Fitness may not read, and the Skill Tree already shows XP per workout | Rejected: an `xpFor` seam on the module contract
2026-09-12 | Fitness keeps its four URL tabs under the artboard's tile strip; the Workouts tab is the artboard's card | Exercises, Body and Plan are built and the artboard predates them | Rejected: dropping the tabs and stacking Plan and Body as cards
2026-09-12 | Fitness's goal tile is the worst-status goal in the Goals digest that shares a skill link with any workout, through `core.digests` and the registry, no skill id hard coded | Fitness reads no goals table; the digest seam is the one Health already uses | Rejected: a featured-goal setting
2026-09-12 | Review offers Undo on a dismissal only; an approval shows its decided line and no button | `reopen()` moves the row back without reversing the write, so approving again would run the tool twice | Rejected: Undo on both with copy saying the write stays
2026-09-12 | A dismissed proposal holds for 30 days: `propose()` returns the dismissed row when the same module, tool and payload comes back inside `dismissed_until` | the Review line "will not re-propose this for 30 days" was drawn but not enforced; enforcing it in core makes the copy true everywhere a proposal is raised | Rejected: dropping the sentence
2026-09-12 | Review copy names what the tool does: "Guarded tool: approving writes to {Module} immediately.", "Approved. {module.tool} ran."; the empty inbox names the nightly hour from the cron in the owner's zone | not every tool sets `is_manual`, so the artboard's line was untrue for most | Rejected: "sets is_manual = true"; a fixed "04:00"
2026-09-12 | Review's tabs wrap the DS TabBar in a client `ReviewTabs`; `TabLinks` is not changed | the screen needed `tabClassName` and TabLinks lacks it; a wrapper in the screen's folder kept the batch's shared-file rule | Rejected: adding `tabClassName` to TabLinks in this batch
2026-09-12 | MetricTile's `leading-none` follows the text size in its class list | tailwind-merge drops a `leading-*` that precedes a text size, so every tile number had been rendering at 51px line height | Rejected: per-screen wrappers around every tile
2026-09-12 | A connected token card has no replace-the-key field; a rotated key means Disconnect, then connect again | the artboard's connected card is the strip and the buttons, and a disconnect loses nothing but a status row | Rejected: the replace field the app had, which made every connected card a form

2026-09-12 | A search hit carries the registry row's updated_at, and the row's meta and the drawer's When cell read "{n} days ago"; no Amount cell | the last change is the one date every module's rows share; nothing in the registry holds money | Rejected: parsing a dollar figure out of the title
2026-09-12 | The empty Search page is the box and every module's chip, nothing else; related rows and the footer both open the entity's module | that is the artboard, and the registry knows the module and not the row's own URL | Rejected: the "Nothing searched" explainer; related rows that do nothing
2026-09-12 | The command palette takes the artboard's geometry: a 640px column at 14vh, the box, then a panel with Results and Go to, G-codes on the right | the behaviour was already the artboard's; the shape was the build's | Rejected: leaving the palette as the one shared piece the pass skipped
2026-09-12 | Below md every Overlay renders as PosPhone's bottom sheet, whatever its side prop: 74vh max, a handle, 18px sides, a 20px title and the 34px home indicator inset; the desktop keeps its right drawers | the phone artboard opens every detail as a sheet and the 2026-09-07 decision already said drawers become sheets, but the sheet geometry sat behind a prop no caller passed, so at 402 every drawer was a full height right slide | Rejected: a full height panel on the phone, which is not what the artboard draws; switching side per caller, twelve callers to keep in step for one rule
2026-09-12 | Notifications draws one channel per alert row and keeps the app's own digest footer | `core.notifications.channel` is one column and nothing parses an email reply, so "PUSH + EMAIL" on one row and "Reply STOP" would both be untrue | Rejected: a core/notify.ts change to record every channel of one send
2026-09-12 | The email preview's recipient is read from settings, then OWNER_EMAIL, else "not set" | the hardcoded address was not true for any fork and not necessarily for the owner | Rejected: leaving the placeholder
2026-09-12 | Agent Log gets "Retry now" on a failed job, running that module's jobs immediately through the existing runNightly({ module }); no header "Undo last run" | the job runner already takes a module and the per-run undo is one click away in the open run; "queued for tonight" would misdescribe an immediate rerun | Rejected: a header undo shortcut; the artboard's queued copy
2026-09-12 | The Jobs rail keeps "{Module} / {job}" with the job name humanised | several modules share job names and the artboard's prose names exist nowhere | Rejected: hand-written job names
2026-09-12 | The onboarding wizard renders in its own route group with no rail, tab bar or palette | the artboard draws nothing around the wizard but its own six steps, and the app shell's rail was doubling it | Rejected: a path check inside the app layout
2026-09-12 | WizardShell takes nextVariant and backVariant; Onboarding passes accent and outline, the weekly review keeps quiet and solid | the two artboards draw different footers on the same shell | Rejected: changing the shell's defaults
2026-09-12 | Onboarding keeps the app's three generic starter goals, adds Target and By when per goal, drops the check-in cadence, and never says "Connected" or promises a backfill | two of the artboard's five goals were personal to its author and the code holds nothing personal; cadence has no field; nothing connects until Settings and 90 days is only true for SimpleFIN | Rejected: porting the five goals; a cadence picker; the artboard's connected panel
2026-09-12 | Onboarding's provider lists stay flat with search and manual add; a NEEDED flag marks Finance's bank and broker categories inside the wizard; no "Calendar & email" category | the artboard's top-5 plus "more" split and its extra category have nothing behind them; the flag lives in the route folder rather than the connectors yaml | Rejected: a yaml field; a category nothing consumes
2026-09-12 | Login never claims "Sent via Resend" and has no footer status dot | the magic link goes through Supabase Auth's mailer, and a pre-auth page answers every visitor identically | Rejected: the artboard's sent-via cell and footer
2026-09-12 | A parallel-batch agent's "match" verdict is not trusted until the integrator has read the pair | Onboarding's first report called every pair a match and none was; the reading is the check, not the report | Rejected: forwarding agent verdicts to Nick unread
2026-09-12 | A long press is 480ms with the pointer moving under 10px, touch and pen only, and it enters the existing dashboard Arrange mode | PosPhone's timer is 480ms; the 2026-09-09 reason for skipping the gesture (no stored tile order) went when the Dashboard pass added Arrange with a saved order, so the gesture is only a way in | Rejected: a phone-only arrange with ↑↓ as PosPhone draws it, a second arrange to keep in step with the first when ‹ › already covers it; firing on mouse, a slow click is not a request to rearrange
2026-09-12 | Goal weight is the sum of weight * confidence over the skill links of goals, low activity is no XP in 30 days, and the skills digest lists up to five such leaf skills heaviest first | it is the number and the rule the Skill Tree page already used for its "Goal weight high, low activity" column, so the digest bullet SPEC asked for is the same list at night; the classifier already writes those links | Rejected: a per-skill weight column on goals, a second number to keep in step with the links; rolling parent weights into the digest, a parent under pressure is its children
2026-09-12 | The globe zooms about the cursor or pinch midpoint through a translate plus scale kept in pure math (zoomAt, clampView), the wheel is a native non-passive listener, wheel down and pinch out zoom in, dragging pushes the land away from the pointer, max zoom is 8x and every pin names itself from 2x at a fixed size on screen | the React wheel handler is passive so the page scrolled under the globe; scaling about the origin made a zoom slide toward the centre; 1.2x per wheel event was one trackpad gesture to the cap; the owner reads the previous drag and wheel directions as reversed | Rejected: pointer capture on the svg, which routes the click to the svg so a pin could never be picked; labels that grow with zoom, which overlap sooner; a place details route, the app's trips open as drawers
2026-09-12 | A grey pin opens the trip its place_visited row points at, and a place with no trip opens a read-only PlaceDrawer | place_visited already carries trip_id and the trip drawer is the detail view; the seeded places have no trip and clicking them did nothing | Rejected: a place drawer that edits, nothing in SPEC edits a visited place
2026-09-12 | Planning files follow the repo's layout: the adoption plan is docs/plans/backend-live.md, the owner list is docs/OWNER-TODO.md, open decisions stay in docs/DECISIONS.md | the repo already has a plans folder, an owner list and a decisions file, and a root PLAN.md beside them would be a second place to look | Rejected: PLAN.md and DECISIONS.md at the root as the /adopt-repo template lays them out
2026-09-12 | AGENTS.md is gitignored | it is a Codex copy of CLAUDE.md for one workspace and the template holds nothing workspace-specific; two files with the same rules drift | Rejected: committing it; a symlink to CLAUDE.md
2026-09-12 | digest_morning_at is a read-only label showing the cron hour in the owner's zone; nothing writes it | the Vercel cron fires at 09:00 UTC from vercel.json and no setting can move it, so a field that looks editable promises a schedule the app cannot keep; the key stays in core/settings.ts so stored rows still parse | Rejected: a second cron per hour offered; removing the key and migrating the row
2026-09-12 | Vercel Authentication is off for production and on for previews | the app's own gate is OWNER_EMAIL plus disabled signup plus RLS, and a Vercel login in front of the magic link breaks the phone flow and the PWA; previews keep it because nothing gates them | Rejected: Vercel Authentication everywhere; Tailscale
2026-09-12 | SPEC is amended for the three tables the migrations collapsed: budgets and budget_lines into finance.budget, meal_log into meals.plan_entry with eaten, bookings into travel.itinerary_item with status | the schema shipped with these shapes and the code is built on them; the spec follows the schema rather than the schema growing three tables to match a document | Rejected: migrations that split them back out
2026-09-12 | Vercel builds and runs on Node 22 | the repo's engines field, CI and the laptop are on 22, and production was the only place on 24; one runtime everywhere or a bug that only reproduces in production | Rejected: moving CI and the laptop to 24
2026-09-12 | A red CI check is a manual merge gate, and pnpm build joins CI | branch protection is unavailable on a private free repo, so the rule is written down instead of enforced; the login defect shipped because CI never ran next build | Rejected: paying for branch protection; a merge queue
2026-09-12 | Apple Health readings land in fitness.body_metric through an optional inbound seam on the module contract, keyed by integration id and dispatched by the generic webhook route after the integration's zod schema validates | the same direction as Strava, the integration translates and the module writes, and a push has no nightly job to pull through; core still names no module and no integration | Rejected: an integration-owned staging table drained nightly (a new concept, a migration, readings a day late); the integration calling callTool (an import cycle through the module index and a proposal for every scale reading at autonomy observe)
2026-09-12 | The cron-silence check is one step at the end of the backup workflow: no clean or partial core.job_runs row finished in the last 36 hours fails the run and GitHub emails the owner | the workflow already runs daily with the database URL and the Postgres client; at 04:10 UTC the last 09:00 UTC run is 19 hours old and a missed one is 43, so 36 is the line; failed is excluded because a run where every job failed sent no digest either | Rejected: a paid uptime service; an error tracker; a second workflow
2026-09-12 | PR #12 (Phase 0, documentation only) merges with the screens CI job red, the one exception to the red-check rule, and making that job green is a Phase 1 task | the e2e suite ran in CI for the first time on this PR (every earlier screens job died at setup:demo without VAPID keys, and main's own run after PR #10 was cancelled), so the 7 failures are main's tests assuming the owner's laptop state, not this branch; holding a docs PR on code it does not touch blocks the plan it carries | Rejected: fixing the 7 tests inside the docs PR; holding #12 until Phase 1
2026-09-12 | Health Auto Export's Test reads the newest 200 on its webhook route from core.request_log, shown in the owner's zone, and the registry test accepts a module's inbound[id] as the webhook's consumer in place of a manifest webhook function | a push has nothing to call, so whether one has arrived is the only fact a Test can report, and withLog already keeps every hit; the no-op webhook that the old assertion required is exactly what the seam replaces | Rejected: a last_payload_at column on core.connections, a second record of what the log already holds; keeping an empty webhook function to satisfy the test
2026-09-12 | Sleep from Health Auto Export is dated by the morning it ended, and a metric's day is the first ten characters of the app's local timestamp, never converted to UTC | a night belongs to the day you wake; the phone already wrote its local day and a UTC conversion would move an evening reading in Chicago to tomorrow | Rejected: dating sleep by sleepStart; parsing the timestamp into a Date
2026-09-12 | CI proves the production login page by serving it, not only by building it | next build exits 0 on the fieldClass defect because /login is dynamic and nothing renders it at build time (verified on the pre-fix code: build clean, served class was the thrown-error string). So the check job runs next start after the build, fetches /login, and fails if the class is missing or "Attempted to call" is present. Rejected: running the e2e suite against next start (a bigger change to playwright.config and the CI screens job, for one page's worth of proof) | Phase 1
2026-09-12 | The push Devices test asserts the configured, no-device state | pnpm setup requires the VAPID keys, so "Push is not configured" is not a state a set-up database reaches; CI writes the keys. The owner's laptop .env still lacks them, so that test fails locally until the pair is added, which the owner list already asks for | Phase 1
2026-09-13 | Apple Health is the workout source for now, through the same Health Auto Export webhook, and the webhook takes every daily reading the app documents (sixteen body_metric kinds) rather than five | the owner has no Strava subscription and did not want to register an app yet; the fitness.workout table already allowed source = 'health_auto_export' and the app's v2 workout has an id to upsert on, so the seam built in Phase 4 covers it without a new concept | Rejected: registering a free Strava app anyway (the owner's call to defer); a second inbound for workouts only
2026-09-13 | A Health Auto Export workout emits workout_logged on first insert only, read from the upsert's xmax | register() always emits for a named event, so a re-send of the same workout id would award its XP twice; the entity still re-registers so its title stays current | Rejected: skipping register() on update (the title would go stale); a deduplicating check on core.events
2026-09-13 | Daily totals from Health Auto Export (steps, energy, exercise, stand, flights, distance) are summed across the points in a payload for one day; levels keep the last reading | the app can be set to hourly buckets, and a day's steps is not its last hour's | Rejected: requiring daily aggregation in the app (a setting nobody would remember); storing one row per bucket (the table is one row per kind per day)
2026-09-13 | Attack Challenge Mode stays off on the Vercel firewall; Bot Protection runs in Log mode | tested on, it answered 429 to every non-browser request, which blocks the Health Auto Export webhook, /api/mcp and possibly the cron; Vercel documents it as a temporary under-attack switch | Rejected: leaving it on with bypass rules (a Pro feature, and a permanent challenge on a single-user app protects nothing the login does not)
2026-09-13 | DATABASE_URL, on Vercel and on the laptop, is a Supabase pooler URL (user postgres.<ref>), never the direct host; production runs on the session pooler, port 5432 | the direct host resolves to IPv6 only and neither Vercel functions nor the owner's network reach it (the setup doc said so; the direct string was pasted anyway and failed twice on 2026-09-13); the session pooler is what the backup workflow already uses, and the transaction pooler on 6543 the doc recommends works as well since no query here is prepared | Rejected: the direct connection
2026-09-13 | A second Apple Health source, apple_shortcuts, takes a flat { day, metrics, workouts } payload from an iOS Shortcut through its own webhook and secret, writing the same fitness rows through a shared modules/fitness/inbound.ts | the owner asked for a free route: Strava requires a paid subscription to create an API app since June 2026, Health Auto Export's automatic sync is a paid tier, and Apple Health is readable only on the phone, which Shortcuts can do for nothing | Rejected: teaching the health_auto_export webhook a second shape (two shapes behind one secret muddle the Test and the docs); a native iOS app (a new codebase and a $99 a year developer account)
2026-09-13 | A Shortcut-posted workout's identity is its start instant | no HealthKit id reaches a Shortcut, and two workouts cannot start at the same second | Rejected: a hash of name and start (no better, and opaque on the row)
2026-09-13 | The phone shell is rebuilt to docs/plans/phone-shell.md: tabs Home, Tasks, Finance, Browse (a page, not a More sheet), a phone header with back and one action, sheets opened from client state with the URL kept by pushState, segments that own their swipe, and app-owned edge swipe-back | the owner uses POS as an installed PWA and every part of the phone shell fails: the desktop PageHeader squeezed to 402px, one server round trip per sheet, no loading state, no safe-area, swipes fighting native scroll. Supersedes the 2026-09-10 entries that named the four tabs from PosPhone, opened More as the artboard's sheet, and declined per-screen reflow. PosPhone no longer specifies the shell; the brand layer still governs type, shape and colour | Rejected: five pinned tabs with More (the overflow is the part that fails); pushing each item to its own route (new routes in eleven modules and a server trip per open); separate phone components per module (double the UI to keep in sync)
2026-09-13 | Pull down reloads the screen (router.refresh) on every page; the nightly agent runs from an explicit button on Home and on the Agent log, on both widths | pull means fetch in every mainstream app, and a job that runs on a gesture with no spinner is invisible. Supersedes the gestures pass entry where pull-to-sync ran the nightly | Rejected: pull runs the job with a spinner (still surprises); no pull gesture at all
2026-09-13 | Phone Home is a Today page of the headline plus five tiles (warnings, review, tasks, finance, timeline); every other tile is desktop only, and Arrange is desktop only | thirteen tiles made a 4900px column. Supersedes the 2026-09-10 entry that kept every tile on the phone | Rejected: every tile compacted to one row (still thirteen decisions per glance)
2026-09-13 | useSearchState asks the router to push or replace the URL and returns the new params on the next render from an override shared across every instance of the hook; it never writes the URL through the History API | a history write makes the router restore a page segment it cached for that URL, and when that write raced a server action the restore showed data from before the action (a deleted task still listed). Attaching the router's own history state to the write cut the rate but still failed about once in thirty runs under a slow network. Router navigations queue in order with actions and passed 54 of 54 under the same delay; the override keeps the sheet instant, and the URL follows one fetch behind, which a test that reloads must wait for | Rejected: bare history.pushState (the race); pushState plus router.refresh (the refresh raced the action the same way); pushState with the router's state attached (rarer, still a race); reordering every close-then-save site (a rule each new screen would have to remember)
2026-09-13 | A Strava activity emits workout_logged on first insert only, read from the upsert's xmax, the same rule the two Apple Health sources follow | the sync re-fetches a 48 hour overlap every night, so most activities are stored on two or three runs, and register() always emits for a named event: each overlap night awarded the XP again | Rejected: shrinking the overlap (a late upload would then be missed); deduplicating in core.events
2026-09-13 | pnpm setup applies local migrations only when DATABASE_URL is the local stack; against anything else it says so and leaves the schema to supabase db push | `migration up --local` reaches the local stack whatever DATABASE_URL says, so the production bootstrap on 2026-09-13 touched the laptop's database, harmlessly that time | Rejected: a --remote flag (one more thing to remember); running db push from the script (it prompts, and the owner runs it by hand anyway)
2026-09-13 | The Skill Tree nebulae are drawn inside the pan and zoom group, one per attribute, so they move with the tree; only the star dust stays fixed behind it | the owner asked for it: anchored to the attributes but drawn behind the transform, the glows stayed put while the stars zoomed away from them and read as stains on the glass rather than light around the tree. Supersedes the "depth without parallax" choice that kept both dust and nebulae fixed | Rejected: moving the dust as well (the sky then has no depth at all)
2026-09-13 | The Skill Tree page returns one element, and on a phone the sky is sized by the tree's aspect ratio with the legend in flow under it | the module route wraps every page in space-y-5, and as two children the band and the full-height grid picked up 20px between them, which was the whole page overrun on desktop; on a phone a 320px sky minimum left 66px of empty sky under the leaves and pushed the four columns under the tab bar | Rejected: subtracting 20px from the grid height (a fix for the symptom that breaks the moment the wrapper changes); a phone section height measured from the viewport and the band (the band's height is not fixed on a phone)
2026-09-13 | The globe's land is filled Natural Earth country polygons, committed as rings in modules/travel/land.json and clipped at the horizon in globe.ts, with borders, a lit-sphere gradient and an ocean tint | the owner found the dot matrix hard to read; the 2026-09-11 dot decision is superseded on the artboard point only, and the no-d3, no-CDN-at-runtime rule from 2026-09-07 holds | Rejected: a photo texture on canvas or three.js (a 1 MB asset and a new dependency); denser dots
2026-09-13 | The globe opens and resets centred on the continental US at 98 W with no tilt | the tilt toward the next trip's latitude read as a broken start | Rejected: centring on the next trip level; a stored home place
2026-09-13 | (Phase 3 of docs/plans/travel-globe.md) City coordinates come from Open-Meteo geocoding, called from a server action in the travel module with no key, shown as a native datalist | no account and no secret means no integration folder; suggestions let the owner disambiguate Paris | Rejected: Nominatim (1 request per second policy); a bundled 1 MB GeoNames list
2026-09-13 | The desktop rail gets no Browse link; Browse is a phone tab only | the rail already lists every module and the palette covers the footer pages, so a Browse row on the desktop would be a second copy of the rail | Rejected: a Browse row in the rail footer
2026-09-13 | phoneTabs lives in core/phone-tabs.ts, not core/nav.ts, and the phone bar's Browse glyph is Compass | core/nav.ts reads the database and the tab bar is a client component, so a value import from it would pull core/modules and core/settings into the browser bundle; LayoutGrid, which the plan named, is already Dashboard's glyph | Rejected: a value export from core/nav.ts
2026-09-13 | On the phone, PageHeader's right slot is phoneAction when given and the desktop actions otherwise | the nine modules with no phone pass of their own keep their header buttons (Insurance's Add policy, Goals's New goal) instead of losing them until a pass exists; the plan's one-action rule is met by the modules that get a pass | Rejected: hiding actions on the phone until every module passes a phoneAction
2026-09-13 | The five screens that draw their own band (Second Brain, Meals, Skill Tree, Travel, Weekly review) mount BackControl in it, and BackControl is phone only by its own class; Fitness passes its SyncBand as the phone action, with the band's reading desktop only so the phone gets the button alone; the shell's rail and tab badge count pending proposals through core countPending, the same count Browse and Review use | the Phase 2 goal is a phone header on every screen, and an installed PWA has no other way back from these five; a pull is the thing to do from a phone after a run; two counts for one badge disagreed by the snoozed proposals | Rejected: leaving the five to their module phases (the owner chose now); a compact prop on SyncBand (a class on the reading is the whole difference); keeping the layout's own count query (the snoozed ones are not a badge anywhere else)
2026-09-14 | Every numeric field is a plain text input with inputMode=decimal, parsed through core/numbers.ts parseNumber, and the Overlay reads onClose through useEffectEvent so its focus effect runs once per open | the owner could only edit a Finance budget limit one step at a time: the Overlay refocused its panel on every keystroke because most callers pass a fresh onClose closure, and type=number blanks a pasted "$1,200" and grows spinner arrows nobody asked for | Rejected: a shared NumberInput component (an attribute swap and one function is smaller and the sites differ in layout); useCallback at every Overlay caller (45 sites, and the next drawer would forget)
2026-09-14 | A dashboard tile the phone does not carry is hidden at its grid cell, through `phone: false` on `Tile`, not at its Card | a hidden Card inside a visible cell still reserved the grid's 200px minimum row, thirteen blank gaps on the phone | Rejected: `hidden md:flex` on each Card as the plan text read; a second tile list for the phone
2026-09-14 | Finance's desktop dashboard is hidden below md with CSS rather than left unrendered through useIsPhone | the plan removes useDesktop and every section the phone shows is the same data, so nothing is fetched twice; a hidden block costs HTML only, and a mobile test that reads text scopes to the segment pane | Rejected: gating the desktop block with useIsPhone (a hydration flash on the phone, the thing Phase 1 removed)
2026-09-14 | On the phone, Tasks' By goal, By project, Review and Done views live behind a Filter button at the end of the segment row as a PillGroup; the header's action stays New task alone | the shell allows one primary action top right, and the three segments Today, This week and Calendar are the daily views | Rejected: the filter behind the header action (bumps New task or makes two actions); seven segments (the row clips at 402)
2026-09-14 | An element that swipes for itself carries data-swipes and the Segments pane declines a press that starts inside one | stopping propagation at the row would also hide the press from PullToRefresh, which listens on the document; a data attribute keeps both gestures and costs one line in the pane | Rejected: event.stopPropagation() in the row; a capture-phase flag on the pane
2026-09-14 | The ink-3 and ink-4 tokens stay as the brand layer sets them although secondary text measures 3.9 to 4.0:1 against AA's 4.5; only the trip form footer moves to ink-2 | colour comes from ComeauxVerse/brand/visual.md, and a token change is a brand decision for the owner to take there, not in a polish pass | Rejected: darkening the two tokens in app/globals.css (every screen changes, the brand layer and the app drift)
2026-09-14 | Phone Home shows two warnings and one proposal, the rest behind an "and N more" link | three and two, the numbers first approved, measured 2037px with the seed against the plan's 1800px; two and one measure 1784px | Rejected: compacting the rows (loses the buttons the tile exists for); leaving the height

